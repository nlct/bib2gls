@entry{sample1,
  name={sample1},
  description={an example that references \gls{sample2}}
}

@entry{sample2,
  name={sample2},
  description={an example that references \gls{sample3}}
}

@entry{sample3,
  name={sample3},
  description={an example that references \gls{sample4}}
}

@entry{sample4,
  name={sample4},
  description={an example that references \gls{sample1}}
}

