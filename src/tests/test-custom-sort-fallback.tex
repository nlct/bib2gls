% arara: lualatex
% arara: bib2gls: { group: on }
% arara: lualatex
\documentclass{article}

\usepackage{fontspec}
\usepackage{siunitx}
\usepackage[record,stylemods=bookindex,style=bookindex]{glossaries-extra}

\renewcommand{\glsxtrbookindexlocation}[1]{%
  (sort value: \texttt{\glsxtrusefield{#1}{sort}}; 
   category: \texttt{\glscategory{#1}};
   original entry type: \texttt{@\glsxtrusefield{#1}{originalentrytype}})
}

\GlsXtrLoadResources[src={test-custom-sort-fallback},
 break-at={none},
 field-concat-sep={/},
% category={same as original entry},
 entry-type-aliases={pictograph=symbol,unit=symbol,obelism=entry},
 custom-sort-fallbacks={
    pictograph=description,
    number=user1,
    abbreviation=long+short,
    unit=symbol,
    entry=description
 },
 save-original-entrytype,
% save-original-entrytype-action=changed,
 selection=all]

\begin{document}
\printunsrtglossaries
\end{document}
