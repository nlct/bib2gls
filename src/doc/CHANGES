
Experimental versions:

0.8a (in progress).

   * New resource options:

     - break-at
     - break-marker

   * New sort options:

     - letter-case-reverse
     - letter-nocase-reverse

   * New commands:

      - \bibglspassimname
      - \bibglspagename
      - \bibglspagesname


0.7a (2017-05-01).

   * Added custom sort rule

   * New resource options:

     - group 
     - loc-counters
     - supplemental-locations
     - supplemental-category
     - supplemental-selection

    * added check for missing openin_any and openout_any 
      (in case of MikTeX);

    * changed inconsistent range errors into warnings

    * New command \bibglsinterloper

    * @symbol and @number always fallback on the label if the
      sort field is omitted.


0.6a (2017-02-24).

   * Added `\bibglspassim`, `\bibglsrange` and `--locale`/`-l`

0.5a (2017-02-09).

   * Added recursive dependency check.

0.4a (2017-02-08).

   * Added `save-locations` option. 
   * Fixed --long-arg=value form.

0.3a (2017-02-04).

   * Fixed `@comment` parsing and `--no-interpret` switch.

